import type { TableColumnKey } from '../types/types';

export const TableColumns: TableColumnKey[] = [
  "city",
  "state",
  "country",
  "postcode",
  "number",
  "name",
  "latitude",
  "longitude",
]